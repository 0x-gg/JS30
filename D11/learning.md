this.dataset.skip - data-skip attribute

parseFloat(someString); // to convert to number

requestFullScreen();
cancelFullScreen();

video event listeners:
play
pause
timeupdate/progress - video emits this event, can be used to track progress bar

video.paused
video.currentTime
video.duration
